# URL without a trailing slash or SAS query string.
AZURE_DATASET_BASE_URL=https://ACCOUNT.blob.core.windows.net/CONTAINER/page-layout/v2-20260822

# Include the leading question mark. Use a short-lived token and never commit it.
AZURE_DATASET_SAS=?sv=REPLACE_WITH_READ_OR_WRITE_SAS

# Optional separate destination for checkpoints and results.
AZURE_RESULTS_BASE_URL=https://ACCOUNT.blob.core.windows.net/CONTAINER/page-layout/results
AZURE_RESULTS_SAS=?sv=REPLACE_WITH_WRITE_SAS

